# Dummy E2E configuration (offline-safe, deterministic).
sources:
  enabled:
    - dummy

notifications:
  telegram:
    enabled: true
    dry_run: false
  dedupe:
    enabled: true
    state_path: last_notified.json

channels:
  top_matches:
    top_n: 2
  data_only_best_picks:
    top_n: 3

digest:
  mode: daily_window
  window_hours: 24
